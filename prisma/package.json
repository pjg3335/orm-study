{
  "name": "prisma-study",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:local": "dotenv -e .env -- npx prisma migrate dev",
    "migrate:dev:init": "dotenv -e .env.development -- npx prisma generate",
    "migrate:dev": "dotenv -e .env.development -- npx prisma migrate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.12.2",
    "dotenv": "^10.0.0",
    "prisma": "^2.24.1",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "@prisma/client": "^2.24.1"
  }
}
